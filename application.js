// Generated by CoffeeScript 1.3.3
(function() {
  var Analyzer, analyzer;

  Analyzer = (function() {

    function Analyzer(dictionary_path) {
      this.afinn = {};
      $.get(dictionary_path, function(err, r, body) {
        var line, lines, _fn, _i, _len;
        lines = body.split('\n');
        _fn = function(line) {
          var value, word, _ref;
          _ref = line.split('\t'), word = _ref[0], value = _ref[1];
          return afinn[word] = value;
        };
        for (_i = 0, _len = lines.length; _i < _len; _i++) {
          line = lines[_i];
          _fn(line);
        }
        return console.log(this.affinn);
      });
    }

    Analyzer.prototype.analyze = function(text) {
      var word, words, _fn, _i, _len;
      words = text.toLowerCase().split(/\W+/);
      _fn = function(word) {
        return console.log(word);
      };
      for (_i = 0, _len = words.length; _i < _len; _i++) {
        word = words[_i];
        _fn(word);
      }
      return console.log('end');
    };

    return Analyzer;

  })();

  analyzer = new Analyzer('AFINN-111-emo.txt');

}).call(this);
